name: 'Rebase nonProd onto master'

on:
  push:
    branches:
      - master

jobs:
  rebase-nonprod:
    runs-on: ubuntu-20.04

    steps:
      - name: "Checkout repository"
        uses: CleverShuttle/gh-composite-actions/actions/rebase-nonprod@v1.6.1
