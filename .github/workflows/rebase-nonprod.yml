name: 'Common: Rebase NONPROD onto default branch'

on:
  workflow_call:

jobs:
  rebase-nonprod:
    runs-on: ubuntu-20.04
    steps:
      - name: "Rebase NONPROD onto default branch"
        uses: CleverShuttle/gh-composite-actions/actions/rebase-nonprod@v1
