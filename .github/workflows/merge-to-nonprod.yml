name: "Common: Merge to NONPROD"

on:
  workflow_call:

jobs:
  merge-to-nonprod:
    runs-on: ubuntu-20.04
    steps:
      - name: "Merge to NONPROD"
        uses: CleverShuttle/gh-composite-actions/actions/merge-to-nonprod@refactor/CP-558_use_merge_instead_of_rebase
