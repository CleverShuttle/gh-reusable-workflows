name: "Common: Merge to nonProd"

on:
  workflow_call:

jobs:
  merge-to-nonprod:
    runs-on: ubuntu-20.04
    steps:
      - name: "Merge to nonProd"
        uses: CleverShuttle/gh-composite-actions/actions/merge-to-nonprod@v1.26.0
