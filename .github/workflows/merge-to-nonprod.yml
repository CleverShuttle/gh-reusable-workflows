name: "Common: Merge to nonProd"

on:
  workflow_call:

jobs:
  merge-to-nonprod:
    runs-on: ubuntu-20.04
    steps:
      - name: "Checkout repository"
        uses: CleverShuttle/gh-composite-actions/actions/merge-to-nonprod@v1.25.0
